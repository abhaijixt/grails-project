# Documentation toolchain for the MkDocs site defined in mkdocs.yml.
# Install into a virtualenv:  pip install -r requirements-docs.txt
# Versions are pinned so `mkdocs build --strict` is reproducible.

mkdocs==1.6.1
mkdocs-material==9.7.7

# Pulled in by mkdocs-material, pinned explicitly because mkdocs.yml
# references them directly (superfences/mermaid, highlight, emoji icons).
mkdocs-material-extensions==1.3.1
pymdown-extensions==11.0.1
Markdown==3.10.2
Pygments==2.20.0
